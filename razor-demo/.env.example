Keycloak__ServerUrl=http://localhost:8080
Keycloak__Realm=test
Keycloak__ClientId=razor-demo-client
# Admin Console → Clients → razor-demo-client → Credentials → Client Secret
Keycloak__ClientSecret=
