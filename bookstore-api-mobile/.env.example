PORT=3043

KEYCLOAK_URL=http://localhost:8080
# Khi dùng tunnel (ngrok) cho WebAuthn, đổi thành domain public (https://xxxx.ngrok-free.dev)
# — bookstore-api-mobile gọi JWKS thẳng qua domain đó, đồng thời cũng là "iss" cần khớp token.
REALM=test
CLIENT_ID=passwordless-demo
