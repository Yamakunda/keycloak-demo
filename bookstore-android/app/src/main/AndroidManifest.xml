<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BookstoreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Bookstore"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic: bookstore-api-mobile (BOOKSTORE_API_BASE_URL) chạy HTTP
             qua IP LAN/10.0.2.2 cho demo. Bỏ khi trỏ backend qua HTTPS thật. -->

        <activity
            android:name=".presentation.ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppAuth bắt redirect từ Custom Tab qua deep link
             com.snp.bookstore:/oauth2redirect (khớp redirectUris client Keycloak) -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.snp.bookstore" android:path="/oauth2redirect" />
            </intent-filter>
        </activity>
    </application>
</manifest>
