PORT=3052

# Backend trung gian giữ client_secret (xem ../bookstore-api-qr)
REACT_APP_API_URL=http://localhost:3053

# Backend đọc sách bằng Bearer access token (xem ../bookstore-api-mobile) — tái dùng
# cho QR flow vì cùng realm "test", chỉ verify chữ ký JWT + issuer, không phân biệt client.
REACT_APP_BOOKS_API_URL=http://localhost:3043
