PORT=3053

# Backend gọi Keycloak nội bộ qua "localhost" (map tới host-gateway bằng extra_hosts trong
# docker-compose.yml) — KHÔNG phải domain QR code sẽ encode, vì verification_uri_complete do
# chính Keycloak sinh ra dựa trên KC_HOSTNAME của nó, không phụ thuộc biến này.
# Nếu test QR bằng điện thoại thật, phải set KC_HOSTNAME=<IP LAN/domain ngrok> khi chạy
# `docker compose up` để verification_uri_complete chứa domain điện thoại resolve được.
KEYCLOAK_URL=http://localhost:8080
REALM=test
CLIENT_ID=qr-login-confidential
CLIENT_SECRET=8cdb63690ed626e792c1b151c96363fc20141b8c5a755ba4
